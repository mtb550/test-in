<idea-plugin>

    <!-- ================================================================== -->
    <!-- Identity                                                           -->
    <!-- Name, version, vendor and compatibility range are declared once in -->
    <!-- build.gradle.kts (intellijPlatform.pluginConfiguration) and        -->
    <!-- patched into this file at build time.                              -->
    <!-- ================================================================== -->
    <id>org.testin</id>
    <resource-bundle>messages</resource-bundle>
    <description><![CDATA[
    <b>Testin</b> brings test case management into the IDE. Design test cases, run them, and report on the results in the same window as the code they cover.
    <br/><br/>
    Everything is stored as plain files in a folder you choose &mdash; readable, diffable, and versioned with Git if you want them to be. There is no server to run, no account to create, and no data leaves the machine.
    <br/><br/>

    <b>Design</b>
    <ul>
        <li><b>Test project tree.</b> Test projects, test sets, packages and runs in a sidebar tree, with create, rename, copy, cut, paste, drag and drop, and undo &mdash; the operations a file tree already taught your hands.</li>
        <li><b>Test case editor.</b> Description, steps, expected result, priority, groups, module, preconditions, reference and test data. Search, filter and paging keep a large suite navigable, and every attribute can be switched off the view when it is not the one you are working on.</li>
        <li><b>Spreadsheet grid.</b> The same cases as a table: multi-cell selection, copy and paste with Excel in both directions, rows that size themselves to their content, and column widths that survive a refresh.</li>
        <li><b>Bulk editing.</b> Descriptions, steps, expected results, groups and more, changed across a whole selection in one dialog.</li>
        <li><b>Details panel.</b> The full test case beside the list, including who created it and when.</li>
    </ul>

    <b>Execute</b>
    <ul>
        <li><b>Test runs.</b> Choose cases into a cycle and record what happened: Passed, Failed or Blocked, with an actual result, bug severity and bug priority on a failure, and per-case durations timed for you. Cases a run never reached are marked Untested when it closes, and a case deleted after it was executed keeps the result it earned, marked Removed.</li>
        <li><b>Automation</b> <i>(with the Java and TestNG plugins)</i><b>.</b> Generate packages, classes and methods from your test cases, run them by class or by method, and move between a case and its code from a gutter icon or a click on the card.</li>
    </ul>

    <b>Share</b>
    <ul>
        <li><b>Reports.</b> Execution reports as HTML, PDF, Word or Excel, with the pass rate, a table per status, and the run's real execution times.</li>
        <li><b>Import and export.</b> Excel, CSV and JSON in and out, plus HTML on the way out. Mapped column by column, with a preview before anything is written.</li>
        <li><b>Git</b> <i>(with the Git plugin)</i><b>.</b> A review dialog listing every pending change &mdash; test cases field by field, test runs, and the markers that make a folder a test set &mdash; then commit and push from the same place. Clone a shared test project, sync with a rebase, revert a single changed field, and switch branches from the panel.</li>
    </ul>

    <b>Compatibility</b>
    <ul>
        <li>IntelliJ IDEA, PyCharm, Rider, GoLand, WebStorm and other IntelliJ-based IDEs, build 261 and later.</li>
        <li>Test management works in all of them. Java code generation, TestNG execution and the Git features appear when those plugins are installed, and are absent rather than broken when they are not.</li>
    </ul>

    <b>Privacy</b>
    <ul>
        <li>No telemetry, no accounts, no analytics. The only network traffic is to the Git remote you configure yourself.</li>
    </ul>

    <b>Team</b>
    <ul>
        <li><b><a href="https://www.linkedin.com/in/mtb550/">Muteb AlMughyiri</a></b> &mdash; author and maintainer</li>
        <li><b><a href="https://www.linkedin.com/in/maalzamil/">Mohammed AlZamil</a></b> &mdash; testing and feature requests</li>
    </ul>

    <b>Support</b>
    <ul>
        <li>How Testin works: <a href="https://mtb550.github.io/test-in/">mtb550.github.io/test-in</a> &mdash; what it does, how each screen works and the rules it keeps</li>
        <li>Questions and ideas: <a href="https://github.com/mtb550/test-in/discussions">github.com/mtb550/test-in/discussions</a></li>
        <li>Issues and feature requests: <a href="https://github.com/mtb550/test-in/issues">github.com/mtb550/test-in/issues</a></li>
    </ul>
]]></description>

    <change-notes><![CDATA[
    <b>2.12.0-alpha</b>
    <ul>
        <li><b>Before you update:</b> three things an older build wrote are not read. A test run recorded with 2.10.0-alpha or earlier kept its results in a file named after its folder, which is why renaming a run lost them; results live in <b>run.json</b> now, so such a run opens with no results. A test project is Active or Inactive &mdash; Archived is gone &mdash; so a project still marked Archived is reported as damaged. And a screenshot pasted with 2.11.0-alpha was written inside <b>run.json</b> as letters; screenshots are picture files beside it now, so those runs show none. Remove such a run rather than repairing it, and a team sharing a test project should update together.</li>
        <li><b>A screenshot of the failure, kept as a picture:</b> <b>Ctrl+V</b> in the error box of the failure form pastes whatever is on the clipboard as a small picture under the box, and the <b>x</b> on it takes it out. Each one is a PNG file beside the test run, named by five characters of its own. So the run file stays small however many are pasted, and Git and a server sync carry them as the files they are. The Details tab links each one and opens it at its real size, no report prints one, and Report Bug attaches them to the GitHub issue.</li>
        <li><b>The actual result is spell checked:</b> a misspelled word in what actually happened is underlined as the IDE underlines one anywhere else, and <b>Alt+Enter</b> offers the corrections and Save to dictionary &mdash; in the failure dialog and in light mode's form. Nothing is corrected unless you pick a correction: what is saved is what you typed.</li>
        <li><b>The page size is remembered:</b> the size typed into the box at the right of the status bar is kept, and every test set editor and test run editor opened afterward starts with it, after restarting the IDE too. An editor already open keeps its own.</li>
        <li><b>The view panel takes the keyboard:</b> <b>Tab</b> moves between its three tabs, a click anywhere in the panel brings the keyboard to it, and the mouse wheel scrolls Details, History and Open Bugs. Each page of the panel says what the panel is called and where <b>Escape</b> closes it.</li>
        <li><b>Fifty-two fixes from reading every package.</b> The ones a tester meets: a drag says <b>Re-sorted</b> only once the new order is written, and a drop that moved nothing stays silent; copying no longer loses a test case waiting to be cut, and a paste no longer moves the cut cases instead of the copied ones; undoing a cut and paste after the source test set was renamed no longer deletes the moved cases; a half-written failure in light mode survives another case reporting its result; a branch named <b>feature/login</b> is checked out under that name rather than as <b>login</b>; a sync with two conflicts asks about both; correcting a cell in the export preview no longer edits the test case behind it, and Cancel leaves it as it was; the browse button beside the SFTP key file opens a chooser; a bulk update says how many test cases it wrote; and a name Windows forbids is refused with a message rather than an IDE error report.</li>
        <li><b>Quieter about what it did:</b> a canceled sync no longer reports itself as failed, a failed push is no longer followed by "up to date with the remote", and a rebase that cannot continue for another reason is no longer offered as a conflict with no file in it.</li>
        <li><b>Steadier underneath:</b> a Refresh while a project is still being read no longer redraws over half of it, a clone is no longer scanned on the thread that draws the IDE, and the Testin folder setting is no longer written back on every project open.</li>
        <li><b>How Testin works, written down:</b> <a href="https://mtb550.github.io/test-in/">mtb550.github.io/test-in</a> has a page for each thing a tester does &mdash; 154 of them, each with the rules it keeps &mdash; a page of every shortcut, the formats on disk, and ten minutes from installing Testin to sending a report.</li>
        <li>Tested by <b>Mohammed AlZamil</b>.</li>
    </ul>
    <br/>
    <b>2.11.0-alpha</b>
    <ul>
        <li><b>Before you update:</b> two things an older build wrote are not read. A test run recorded with 2.10.0-alpha or earlier kept its results in a file named after its folder, which is why renaming a run lost them; results live in <b>run.json</b> now, so such a run opens with no results. And a test project is Active or Inactive &mdash; Archived is gone &mdash; so a project still marked Archived is reported as damaged. A team sharing a test project should update together.</li>
        <li><b>Report a failure as a GitHub issue:</b> a failed test case in a test run has <b>Report Bug</b> in the Bug Issue row of its Details tab. Testin writes the bug in one template from the test case, its test run and what the run recorded, and opens it to be read and edited. <b>Send</b> files it with GitHub's <b>gh</b> command line tool, with the screenshots pasted into the error attached. The issue stays linked beside the result, and a test case already reported cannot be reported twice. <b>bugRepoUrl</b> in <b>testin.yml</b> names the repository; Testin stores no password and no token.</li>
        <li><b>Every report links the bug:</b> the PDF, HTML and Word reports show a reported failure's issue as <b>(#12)</b> right after its actual result, and the spreadsheet gives it a Bug Issue column of its own. Clicking it opens the issue.</li>
        <li><b>Light mode:</b> one button on the run editor's toolbar, drawn as a sun, opens a small window that stays above every other window and shows one test case at a time &mdash; so a tester working in the application under test records a verdict without going back to the IDE. It carries the verdict buttons, Start and Stop, the test case and test run clocks and the failure form; <b>Ctrl+D</b> shows the rest of the case and the wheel changes the text size. It moves between test cases instead of snapping, and holds still when the IDE's own Animate windows setting is off.</li>
        <li><b>Every action is in Find Action and the Keymap:</b> one of sixty-six actions was declared to the IDE, so the rest could not be searched for or given a key. All of them are declared now, and a menu prints whatever key the keymap holds, including one you rebound. On macOS the tree cuts and pastes with Cmd, as it already copied.</li>
        <li><b>Change a test run after it has started:</b> Edit Test Run, on an open run, adds and removes test cases, renames the run and changes its configuration, keeping the verdicts of the test cases it still covers. Renaming a run no longer loses its results, and a run can be removed whatever its status.</li>
        <li><b>A group is a word you type,</b> completed from the groups the project already uses, instead of eight tick boxes.</li>
        <li><b>See what is automated:</b> the navigate icon on a test case says whether it has test code, the filter asks it of a whole test set, and a test set's details say how much of it is automated.</li>
        <li><b>The grid answers the keyboard:</b> Enter edits a cell or says why it cannot be edited, Ctrl+C copies the cells you picked, the Context Menu key opens the menu, and pasting a block says so once, with a count.</li>
        <li><b>A run keeps the work done in it:</b> judging every test case a filter showed no longer turned the hidden ones untested and completed the run; Start on an empty filter no longer completes it; closing the tab stops the automation instead of leaving it recording into nothing; an automated re-run that clears failure notes written by hand says what it cleared. Escape closes the failure form without asking, and what was typed is gone.</li>
        <li><b>Nothing lost around it:</b> undoing a cut and paste no longer deletes the test cases from both test sets; Refresh and selecting a hidden test case keep the filters and the search; Escape no longer empties the system clipboard; an import that fails part way says what it already wrote; exporting a package reaches every level under it; copying a test set whose files were named by hand gives each copy an id of its own; and test cases in a folder with no marker are named in a warning instead of silently skipped.</li>
        <li><b>Syncing keeps what you are doing:</b> a Git sync refreshes the editors it changed, and a file changed outside Testin is read when it arrives, including a hand edit made seconds after Testin saved. A verdict, a grid edit or a completed run recorded while an SFTP sync brings that run in lands on the run that arrived. Cancel stops an SFTP sync and an import, Keep on a file the server deleted keeps it, and the SFTP password box shows dots.</li>
        <li><b>Undo is one gesture:</b> Ctrl+Z takes back a drag together with the test code it moved, removing forty test cases is one entry, and an undo that is refused does not spend the press.</li>
        <li><b>Generated code:</b> two descriptions that differ only in punctuation no longer share one method, a folder named for a Java keyword no longer generates a class that will not compile, a drag no longer moves Testin's methods above a <b>@Test</b> you wrote, and a method's name no longer depends on the machine's language.</li>
        <li><b>Testin says what happened:</b> places that refused or acted in silence now say so, once; an automated run reports one line at the end rather than one per test case; every action carries an icon; the two Testin tool windows have different names; and the settings page answers the search and refuses a Testin folder that does not exist.</li>
        <li><b>How Testin works, written down:</b> <a href="https://mtb550.github.io/test-in/">mtb550.github.io/test-in</a> has a page for each thing a tester does &mdash; 153 of them, each with the rules it keeps &mdash; a page of every shortcut, and ten minutes from installing Testin to sending a report.</li>
        <li><b>French and Hindi:</b> every message Testin shows is translated. The IDE offers neither language in its settings, so they show when the IDE runs with the JVM option <b>-Duser.language</b> set to French or Hindi.</li>
        <li>The source is licensed under the Apache License 2.0.</li>
        <li>Tested by <b>Mohammed AlZamil</b>.</li>
    </ul>
    <br/>
    <b>2.10.0-alpha</b>
    <ul>
        <li><b>Order is the rank a test case carries, and nothing else:</b> cases used to store it as a chain across their files, and 2.8 and 2.9 rewrote that chain as a rank on each case the first time they indexed a set. The converter is gone, and with it the only place indexing wrote test data &mdash; a scan is a pure read again. A case that arrives with no rank, whether from an old project, an import or a file copied in by hand, sorts after the ranked ones and oldest first, which is what an unranked case has always done. Nothing is hidden and nothing is refused.</li>
    </ul>
    <br/>
    <b>2.9.3-alpha</b>
    <ul>
        <li><b>A badge looks like what it is, instead of saying so:</b> a pill spent half its width naming the field its color already meant &mdash; "Priority: High" beside "Bug Priority: High" and "Bug Severity: Major". Priority is the value now, and it is <b>P1</b> and <b>P2</b> rather than High and Medium, so it cannot be read as the bug's. P3 draws nothing at all, because P3 is what a case is unless somebody said otherwise. Groups are ribbons rather than pills, since eight identical pills on one row was the hardest row in the plugin to read. Severity and bug priority are one badge carrying the IDE's own bug mark, and each half leaves on its own when its column is unticked.</li>
        <li><b>The next cycle starts from the one before it:</b> Re-create, on a test run in the tree, opens the create dialog holding the previous cycle's cases and its configuration with the next name suggested &mdash; cycle-1 offers cycle-2. Building cycle 2 no longer means re-ticking every case and retyping every answer. No verdict, duration, actual result, stacktrace or bug field is carried across.</li>
        <li><b>A disabled test case stops running:</b> Disabled is the one status that says whether a case should run, and it said it to Testin alone &mdash; the card showed it and the suite ran the case anyway. The generated <b>@Test</b> carries <b>enabled = false</b> now, and leaving Disabled takes the attribute off rather than writing the word that says nothing.</li>
        <li><b>A case you only looked at is not a case you edited:</b> opening a field in the update dialog and pressing Enter without changing anything recorded the case as modified &mdash; Modified By became whoever pressed Enter, and the file was rewritten. The stamp is now decided by whether the save would leave the file different, in the one place every save passes through, so the update dialog, a grid cell, the details panel and a paste are all covered.</li>
        <li><b>A description nobody wrote is empty, and names no method:</b> a blank description was stored as the literal <b>EMPTY_DESCRIPTION</b>, which the tester then read as the case's name on the card, in the grid, in every report and in the execution log, with a generated method called emptyDescription. It is stored empty and drawn blank. Writing a description on such a case creates its method then, and the second unfinished case in a set no longer collides with the first.</li>
        <li><b>The search has a button:</b> the keystroke reaches the search from anywhere, which is also why nothing on screen said the search existed. The project tree's toolbar carries it, under the magnifier the dialog uses, with a tooltip naming whatever key the keymap currently holds &mdash; including one the tester rebound.</li>
        <li>Editing a grid cell re-measured every row on the page, and dragging a column divider did it again on every pixel of the drag &mdash; nine hundred cell layouts on a fifty-row page. The edit measures the rows that changed, the column gestures coalesce, and the cell borders are constants instead of two thousand seven hundred objects per pass.</li>
    </ul>
    <br/>
    <b>2.9.2-alpha</b>
    <ul>
        <li><b>Ctrl+Z reaches the test case, on the surface it is pressed on:</b> undo reached a node move and a node rename and nothing else, so a Remove wrote the deletion straight through and a bulk edit rewrote forty files in one gesture that could not be taken back. All of it is undoable now, and every test editor keeps its own history beside the tree's &mdash; a tester with two editors open takes back what they did here, not whichever change happened to be last. Removing a node is undoable at last. A restore never overwrites work it did not remove, and never stamps the tester as having modified a case at the moment they un-modified it.</li>
        <li><b>Deleted test data goes to the recycle bin:</b> a removed test case was a file erased and a removed test set a folder erased with everything under it, with the confirmation dialog the only thing between a misclick and typing the work in again. Both go to the desktop's own bin now, found in Explorer and restored the way every other file is, without needing this plugin to be working.</li>
        <li><b>A long card title wraps instead of running off the card,</b> in both the test and the run editor. The title is a wrapping text area rather than a label, so a description holding a <b>&lt;</b> is the tester's own sentence rather than markup that swallows the rest of the line.</li>
        <li><b>Test data is shown as it was typed, commas and all:</b> a rendering rule written for comma-separated lists turned every ", " into a line break on the way to the screen. Test data is a username, a query or a payload &mdash; values that are used rather than read &mdash; so each comma vanished between the save and the details panel while the file on disk had kept it all along.</li>
        <li><b>A bulk edit stops flattening the value it saves:</b> a multi-line expected result or test data edited in a bulk dialog was written back as one line, silently, with nothing to undo it. A line break is now written as the two characters that stand for it, so the value still sits on one editor line and comes back whole.</li>
        <li><b>Generated code follows the case in and out:</b> undoing a removal used to put the JSON back and leave the method deleted, and redoing a creation left the method standing with no case. And an import over a class automated by hand stopped appending stubs beside the real tests &mdash; the generator matched method names exactly, so "verify NEXT db value" did not recognize verifyNEXTDbValue, and 84 of one class's 150 descriptions got an empty duplicate.</li>
        <li><b>Refresh says it refreshed,</b> when the cases are back on screen rather than when the button went down &mdash; the read waits for indexing and finishes on another thread, so a balloon at click time announces a refresh that has not happened.</li>
        <li><b>Double-clicking a card leaves the focus on the details tab,</b> so F2 opens the case for editing straight away instead of doing nothing until the tester clicks into the panel first.</li>
        <li>FQCN and Path start visible on the editor toolbar, so the editor shows where a case's automation lives and where the case sits in the tree without being asked.</li>
        <li><b>Stability:</b> the plugin's one private interface method compiled to a call the JVM is entitled to reject &mdash; an error waiting for a JVM that checks, which the Plugin Verifier reported against all four IDEs. It is inlined, and the verdict is Compatible everywhere with no compatibility problems.</li>
    </ul>
    <br/>
    <b>2.9.1-alpha</b>
    <ul>
        <li><b>The stop no longer depends on internal API:</b> finding the process behind a run went through a method the platform marks internal, which can change or disappear in any release with no warning - and it was the load-bearing call in the only feature that can stop a run. The run now keeps the handle the IDE offers it as each process starts, so stopping uses what it was given rather than asking the platform to find it again.</li>
        <li>The grid stopped detaching a hover listener it no longer needed: the cell colors are already set after the platform has had its turn, so the tint it was there to prevent could not appear anyway.</li>
        <li>Every compatibility check the IntelliJ Plugin Verifier offers now fails the build, rather than the two it had been narrowed to.</li>
    </ul>
    <br/>
    <b>2.9.0-alpha</b>
    <ul>
        <li><b>Zero compatibility problems, on every IDE:</b> the Java and TestNG code moved into content modules the IDE loads only where they apply. The plugin page reported 159 problems on PyCharm, GoLand and WebStorm &mdash; 53 apiece &mdash; because the verifier could not see past a runtime guard. It now reports none, on all four.</li>
        <li><b>A run records what happened, not just that it happened:</b> verdicts are given from the keyboard in the grid, the tester types the actual result beside them, and a failure keeps its stacktrace whole. Each case is timed &mdash; including one that passes inside a second &mdash; and the timings survive stopping, resuming and reopening. The details panel shows what the run recorded next to what the case says.</li>
        <li><b>Start a run from the tree:</b> a test set or a whole package starts a run and the verdicts land in it. One runner for one case and for a hundred, so a selection compiles once and executes in the order the tester arranged.</li>
        <li><b>A stop that really stops:</b> the process is killed rather than asked to end, a case that never started is no longer claimed by the run that asked for it, and a run is over when every case has a verdict, whoever gave it.</li>
        <li><b>Execution order belongs to the tester, and the code follows it:</b> a test case carries its own position instead of naming its neighbors, so dragging one case writes one file rather than renumbering the set. The generated <b>@Test</b> carries that position &mdash; priority is a Testin field again, shown, filtered and reported, and no concern of the automation. Order also joins the update menu, so a case can be moved to a place by number.</li>
        <li><b>A filter no longer renumbers the set:</b> cards and grid rows show a case's place in its test set, which stays the same with a filter on. Dragging while a filter was on used to move cases the tester never touched, and save them.</li>
        <li><b>A test project can live on a server, with no Git at all:</b> <b>testin.yml</b> says where. A sync claims the project, merges, and gives the lock back even if it dies mid-claim; a case one side deleted and the other edited is left to the tester instead of stopping the whole sync.</li>
        <li><b>Git that survives a conflict:</b> a conflicted test case is merged field by field and usually asks nothing. A failed push leaves a way forward and names the files; switching branch asks before carrying uncommitted work across, and open editors follow the index rather than the branch they were opened on. A rename is committed as a move rather than as a new file beside the old one.</li>
        <li><b>One key finds anything in the test project</b> &mdash; a test case, a set, a run, a node &mdash; and goes there.</li>
        <li><b>The test case dialog, field by field:</b> Expected Result is multi-line, wears the same frame and focus ring as the surrounding fields, and grows as lines are added. Every field is set in one size. From a selected card, each field's letter opens that field's editor with no menu in between. A bulk edit writes the rows the tester actually edited and leaves the rest byte-identical.</li>
        <li><b>Imports and copies stop freezing the IDE:</b> an import that took 49 seconds with the processor idle now waits for the index once and says where the time went, and a set of test methods is generated as a set in one edit instead of one method at a time. Copying a test set runs under a progress bar.</li>
        <li><b>Reports that agree with each other:</b> the three formats of one run now say the same thing, a report says what it is from its file name, a run named "Sprint 7 R&amp;D" no longer breaks its own report, and what the tester writes about a result is kept with the run and rendered into every format.</li>
        <li><b>Generated code that compiles:</b> a description with a comma in it, two descriptions that differ only in punctuation, and a quoted phrase each used to produce a class that would not build. Go To Code opens the method that carries the case rather than one named like it, and a description that cannot name a test method is now refused where it is typed.</li>
        <li><b>Credentials stay out of the log:</b> an HTTPS remote can carry a token in its URL, and Git echoes that URL when it fails. It no longer reaches <b>idea.log</b> or the balloon on screen.</li>
        <li><b>Stability:</b> the tree no longer waits for the index while holding the read lock, which could take the IDE down with it. Two open projects no longer share one view panel or one editor zoom. Typing in the Status cell no longer throws, and a corrupt manifest no longer reads as though the tester had deleted everything.</li>
        <li><b>One word for one thing:</b> a refusal is red and an outcome is blue everywhere, and every status is captioned by the enum that owns it &mdash; so renaming one reaches the reports, the tree, the grid and the details panel together.</li>
        <li>Tested by <b>Mohammed AlZamil</b>.</li>
    </ul>
    <br/>
    <b>2.8.0-alpha</b>
    <ul>
        <li><b>Every change is reviewable, and the review commits and pushes:</b> the pending-changes dialog now understands test runs and markers as well as test cases, so an executed run no longer shows as a nameless row and a run edited after its first commit no longer disappears from the list entirely. A Test Set column says where each change lives, and the button is <b>Commit &amp; Push</b> with Commit alone behind it &mdash; the push is chosen with the changes still on screen, and the notification names the commit it sent.</li>
        <li><b>Removed:</b> a run keeps the row for a test case deleted under it, marked Removed, carrying the verdict and timings it recorded. The row takes no new verdict &mdash; a case that is gone cannot be run &mdash; execution steps over it, and the reports count it in a table of its own.</li>
        <li><b>Removing a test project works again</b> from the tree, behind a confirmation that says how much goes with it: "Holds 3 test sets, 42 test cases and 5 test runs".</li>
        <li><b>Dates read the way they are written:</b> exporting a sheet and importing it back kept every date, instead of turning each one into today. An imported case now keeps the author and dates its file carried rather than being stamped with whoever ran the import.</li>
        <li><b>Audit that tells the truth:</b> all seven node markers share one audit block, every one of them reads a file written before the fields were renamed, and a node nobody has modified reports when it was made instead of the moment it was read.</li>
        <li><b>Lighter lists:</b> a card reuses its badges instead of rebuilding them for every row, so sorting, filtering and paging a full page of test cases costs a fraction of what it did.</li>
        <li><b>Quieter, clearer messages:</b> import, export, report and sync outcomes appear as status-bar balloons instead of entries in the notification log, which is left for the failures worth finding again. Syncing a project that is not under Git says so plainly and points at where a repository is created.</li>
        <li><b>Shown, not broken:</b> in IDEs without the Java or TestNG plugin, Run Test Case, Run Tests, Automate Test Case and Navigate To Code stay on the menu, grayed, with the plugin each needs written into its name, rather than appearing and refusing.</li>
        <li>The Git identity and group-selection dialogs moved onto the shared dialog framework, so every key they bind is shown; an empty list says why it is empty; and a marker that has not been written yet is no longer logged as an error.</li>
        <li><b>Git on a repository of its own:</b> test cases no longer have to live inside the automation project. The pending review reads Git directly, so a newly written test case shows up straight away, the markers that make a test set a test set travel with the commit, and a first push to an empty repository succeeds.</li>
        <li><b>A real review dialog:</b> pending changes open in a table with a commit message field, a status bar showing the keys, and right-click to revert a single changed field.</li>
        <li><b>Execution timing:</b> a run records when it started and ended, per-case durations survive stopping and resuming, and all three report formats show a real execution date instead of the day the run was created.</li>
        <li><b>Bug severity and priority as badges</b> on the run card, each captioned so it cannot be mistaken for the test case's own priority, and readable in both light and dark themes.</li>
        <li>Tested by <b>Mohammed AlZamil</b>.</li>
    </ul>
    <br/>
    <b>2.7</b>
    <ul>
        <li><b>Multi-IDE Support:</b> Java, TestNG, and Git are now optional dependencies — Testin installs and runs on PyCharm, GoLand, WebStorm, and other IntelliJ-based IDEs. Features that need a missing plugin degrade gracefully with a clear notification instead of failing.</li>
        <li><b>Accurate Metadata:</b> Plugin metadata is declared once in the build; the compatibility statement is verified against each supported IDE by the JetBrains Plugin Verifier.</li>
    </ul>
    <br/>
    <b>2.6</b>
    <ul>
        <li><b>Excel-Style Grid:</b> Row selection from the sequence column, multi-row and multi-cell selection, Excel-compatible copy/cut/paste, and automatic row-height fitting after paste.</li>
        <li><b>Layout Memory:</b> User column widths survive refreshes; the list view opens by default and loads pages asynchronously.</li>
        <li><b>Safer Persistence:</b> Run status writes go through a single sequential writer, eliminating corrupted JSON under rapid status changes.</li>
        <li><b>Zero-Setup Automation Path:</b> The Java test source root is detected automatically at startup — no manual configuration.</li>
        <li>Many stability fixes: dialog lifecycle cleanups, unified keyboard shortcuts, and grid editing edge cases.</li>
    </ul>
    <br/>
    <b>2.5</b>
    <ul>
        <li><b>Grid View:</b> Content-based automatic column sizing with word wrap, uniform cell padding, and inline editing.</li>
        <li><b>Run Creation Redesign:</b> Collapsible sections, clearer layout, and wider input fields in the test run dialog.</li>
        <li><b>Font Sync:</b> Editor zoom is mirrored across the view panel for a consistent scale.</li>
        <li>Drag &amp; drop fixes in the project tree; newly created test cases are selected and scrolled into view.</li>
    </ul>
    <br/>
    <b>2.4.1</b>
    <ul>
        <li><b>Instant HTML Reporting:</b> Added a one-click action to instantly generate and view clean, formatted HTML execution reports right from the toolbar.</li>
        <li><b>Native Event Migration:</b> Migrated background watchers to the verifier-safe postprocessor API to ensure full architectural compatibility with IntelliJ 2025.1+ and 2026 EAP editions.</li>
        <li><b>Performance Refactoring:</b> Optimized list rendering cycles by caching custom row boundaries and styling components, cutting down memory allocation overhead.</li>
    </ul>
    <br/>
    <b>2.0.0</b>
    <ul>
        <li><b>Initial Architecture Release:</b> Introduced the core Test Project Tree sidebar navigation and dynamic, in-memory collection handlers.</li>
        <li>Added the interactive Test Case Editor with pagination, background async loaders, and text query filtering.</li>
        <li>Introduced the Test Run Manager tracking live test item execution state, manual status overrides, and runtime duration counters.</li>
        <li>Integrated global application setting configurations under the IDE Tools menu group.</li>
    </ul>
]]></change-notes>

    <!-- ================================================================== -->
    <!-- Dependencies                                                       -->
    <!-- Only the platform is required. Java, TestNG and Git are optional   -->
    <!-- so Testin loads in PyCharm/GoLand/WebStorm; the code checks        -->
    <!-- org.testin.services.OptionalPlugin before touching their classes.  -->
    <!-- ================================================================== -->
    <depends>com.intellij.modules.platform</depends>
    <depends optional="true" config-file="testin-java.xml">com.intellij.modules.java</depends>
    <depends optional="true" config-file="testin-testng.xml">TestNG-J</depends>
    <depends optional="true" config-file="testin-git.xml">Git4Idea</depends>

    <!-- ================================================================== -->
    <!-- Content modules                                                    -->
    <!-- Code that needs an optional plugin lives in a module of its own,   -->
    <!-- which declares that dependency itself. The Plugin Verifier then    -->
    <!-- checks the module only against IDEs that have it, instead of       -->
    <!-- reporting 53 unresolved classes against every IDE without Java     -->
    <!-- - which is what the Marketplace shows on the plugin page (#144).   -->
    <!-- loading="optional": an IDE without the plugin skips the module.    -->
    <!-- ================================================================== -->
    <content>
        <module name="Testin.testin-java" loading="optional"/>
        <module name="Testin.testin-testng" loading="optional"/>
    </content>

    <!-- ================================================================== -->
    <!-- What a content module contributes back                             -->
    <!-- The core cannot call into a module, so a module hands its work     -->
    <!-- over here. An IDE without the module leaves the list empty, which  -->
    <!-- every reader treats as an answer rather than an absence (#144).    -->
    <!-- ================================================================== -->
    <extensionPoints>
        <extensionPoint name="testRunners"
                        interface="org.testin.runner.TestRunner"
                        dynamic="true"/>
        <extensionPoint name="codeGenerators"
                        interface="org.testin.codegen.CodeGenerators"
                        dynamic="true"/>
        <extensionPoint name="codeNavigation"
                        interface="org.testin.navigate.CodeNavigation"
                        dynamic="true"/>
    </extensionPoints>

    <!-- ================================================================== -->
    <!-- Extensions (platform-only; Java-dependent ones live in             -->
    <!-- testin-java.xml)                                                   -->
    <!-- ================================================================== -->
    <extensions defaultExtensionNs="com.intellij">
        <!-- Tab titles of Testin editors use the normal foreground, not the
             "unknown file" color their non-physical virtual file would get -->
        <editorTabColorProvider implementation="org.testin.editor.TestinTabColorProvider" order="first"/>

        <!-- Notices test data changing on disk - a hand edit, a pull, a branch
             switch - and reads the projects that changed again (#20) -->
        <vfs.asyncListener implementation="org.testin.indexer.TestinFileWatcher"/>

        <!-- First run activity -->
        <postStartupActivity
                implementation="org.testin.setting.StartupActivity"
        />

        <!-- File editors -->
        <virtualFileSystem
                key="testin"
                implementationClass="org.testin.editor.TestinFileSystem"
                physical="false"
        />

        <fileEditorProvider
                implementation="org.testin.editor.UnifiedEditorProvider"
        />

        <!-- Editor Drop Handler -->
        <fileDropHandler
                implementation="org.testin.explorer.tree.TreeDropHandler"
        />

        <!-- Services -->
        <applicationService
                serviceImplementation="org.testin.setting.AppSettingsState"
        />

        <!-- Tool windows -->
        <toolWindow
                id="testin.tree"
                factoryClass="org.testin.explorer.Main"
                anchor="left"
                icon="/icons/pluginIcon.svg"
                secondary="false"
                doNotActivateOnStart="true"
        />

        <toolWindow
                id="testin.view"
                factoryClass="org.testin.view.ViewToolWindowFactory"
                anchor="right"
                icon="/icons/pluginIcon.svg"
                secondary="false"
                doNotActivateOnStart="true"
        />

        <!-- Notifications -->
        <notificationGroup
                id="testin.notifications"
                displayType="BALLOON"
                isLogByDefault="true"
                bundle="messages"
                key="testin.display.name"
        />

        <!-- Settings: application-level, because the state behind the page always
             was. One page for the IDE, so two open projects cannot hold two sets
             of fields over one settings file and overwrite each other (#70). -->
        <applicationConfigurable
                instance="org.testin.setting.SettingsConfigurable"
                id="org.testin.setting.SettingsConfigurable"
                key="testin.display.name"
                bundle="messages"
                groupId="tools"
        />

    </extensions>

    <!-- Every action a tester can reach, declared so Find Action offers it by
         name and Settings -> Keymap can rebind its key (#119).

         An action with a <keyboard-shortcut> owns that key across the IDE and
         grays itself outside Testin. An action without one is still declared:
         it is listed in the Keymap with no default. The surface it belongs to
         registers the key on it, because a declared shortcut is dispatched
         before a component's input map. Declared, it would take ENTER, DELETE or
         the three clipboard keys away from the tree, the card list or the grid.
         Rule-INTERNAL-066, and docs/shortcuts.md says which keys are which.

         The search was the first of these and is still the plainest: it has to
         open from the tree, from either editor and from the view panel alike,
         so it lives in the keymap (#29).

         A key the IDE already owns is a key Testin loses. The platform gathers
         every action bound to a keystroke, runs whichever one is enabled, and
         says nothing when two are - and a component input map cannot rescue it,
         because a registered shortcut is dispatched first. That is how
         CTRL+SHIFT+C quietly did CopyPaths in a test editor for weeks.
         DeclaredShortcutsTest reads this file to check no two Testin actions
         ever claim one key by accident.

         Copy, Cut and Paste Test Case carry no key at all. They were moved off
         CTRL+SHIFT to ALT+SHIFT to stop losing that fight, and then the fight
         turned out not to be worth having: they are context menu entries, and
         a tester who wants them clicks them. They are still declared, so Find
         Action offers them and a tester who does want a key can set one. -->
    <actions>
        <action
                id="Testin.Search"
                class="org.testin.search.GlobalSearchAction"
                icon="AllIcons.Actions.Search"
        >
            <keyboard-shortcut keymap="$default" first-keystroke="ctrl alt F"/>
            <keyboard-shortcut keymap="Mac OS X 10.5+" first-keystroke="meta alt F"/>
        </action>

        <!-- The tree's actions. Declared rather than constructed so Find Action
             finds them and Settings -> Keymap can rebind them; each reads what
             the tree has selected from TestinData and grays itself everywhere
             else, so a key declared here is inert outside Testin (#119). -->
        <!-- The editors' actions. Each reads what the editor has selected from
             TestinData and grays itself outside one (#119). -->
        <action
                id="Testin.CreateTestCase"
                class="org.testin.testcase.CreateTestCaseAction"
                icon="AllIcons.Actions.AddToDictionary"
        >
            <keyboard-shortcut keymap="$default" first-keystroke="ctrl M"/>
            <keyboard-shortcut keymap="Mac OS X 10.5+" first-keystroke="meta M"/>
        </action>

        <action
                id="Testin.UpdateTestCase"
                class="org.testin.testcase.UpdateTestCaseAction"
                icon="AllIcons.Actions.Edit"
        >
            <keyboard-shortcut keymap="$default" first-keystroke="F2"/>
        </action>

        <!-- One entry per verdict, generated from TestStatus. No key here: P, F
             and B carry no modifier, so a keymap entry would answer them
             everywhere in the IDE - the run editor's list binds them (#119). -->
        <group
                id="Testin.SetTestCaseStatus"
                class="org.testin.testrun.SetTestCaseStatusGroup"
                popup="false"
        />

        <action
                id="Testin.UpdateRunItem"
                class="org.testin.testrun.UpdateRunItemAction"
                icon="AllIcons.Actions.Edit"
        >
            <keyboard-shortcut keymap="$default" first-keystroke="F2"/>
        </action>

        <action
                id="Testin.RemoveTestCase"
                class="org.testin.testcase.RemoveTestCaseAction"
                icon="AllIcons.Actions.DeleteTag"
        />

        <action
                id="Testin.PasteTestCaseNode"
                class="org.testin.clipboard.PasteTestCaseNodeAction"
                icon="AllIcons.Actions.MenuPaste"
        />

        <action
                id="Testin.CopyTestCaseNode"
                class="org.testin.clipboard.CopyTestCaseNodeAction"
                icon="AllIcons.Actions.Copy"
        />

        <action
                id="Testin.CutTestCaseNode"
                class="org.testin.clipboard.CutTestCaseNodeAction"
                icon="AllIcons.Actions.MenuCut"
        />

        <action
                id="Testin.CopyTestCase"
                class="org.testin.clipboard.CopyTestCaseAction"
                icon="AllIcons.Actions.Copy"
        />

        <action
                id="Testin.AutomateTestCase"
                class="org.testin.codegen.AutomateTestCaseAction"
                icon="AllIcons.Actions.IntentionBulb"
        >
            <!-- F12 is JumpToLastWindow in the IDE's own keymap, and it is kept
                 anyway: a shared key is fine where it works, and no code goes in
                 to win it. CTRL+F12 was worse - FileStructurePopup owns that and
                 there was no Mac entry at all, so generating code was unreachable
                 by key on a Mac. Rule-INTERNAL-069 says the opposite and this is
                 the exception to it, decided deliberately. -->
            <keyboard-shortcut keymap="$default" first-keystroke="F12"/>
            <keyboard-shortcut keymap="Mac OS X 10.5+" first-keystroke="meta F12"/>
        </action>

        <action
                id="Testin.RunTestMethod"
                class="org.testin.runner.RunTestMethodAction"
                icon="AllIcons.RunConfigurations.TestState.Run"
        >
            <keyboard-shortcut keymap="$default" first-keystroke="F5"/>
        </action>

        <action
                id="Testin.NavigateToTestMethod"
                class="org.testin.navigate.NavigateToTestMethodAction"
                icon="AllIcons.General.ArrowRight"
        >
            <keyboard-shortcut keymap="$default" first-keystroke="shift F5"/>
        </action>

        <action
                id="Testin.NavigateToTestCase"
                class="org.testin.navigate.NavigateToTestCaseAction"
        />

        <action
                id="Testin.ViewDetails"
                class="org.testin.view.ViewDetailsAction"
                icon="AllIcons.Actions.PreviewDetails"
        />

        <action
                id="Testin.CreateNode"
                class="org.testin.creator.CreateTreeNodeAction"
                icon="AllIcons.General.Add"
        />

        <action
                id="Testin.EditTestRun"
                class="org.testin.testrun.EditTestRunAction"
                icon="AllIcons.Actions.Edit"
        >
            <!-- The third action on F2, and the same argument as the two before
                 it: F2 changes the thing in front of you, and which thing that
                 is depends on where you are standing. Update Test Case answers
                 in the test editor, Failed Test Case Details in the run editor,
                 and this one in the tree. Each is gray where the others answer,
                 so only one can ever take the key. -->
            <keyboard-shortcut keymap="$default" first-keystroke="F2"/>
        </action>

        <action
                id="Testin.ReCreateTestRun"
                class="org.testin.testrun.ReCreateTestRunAction"
                icon="AllIcons.Actions.Refresh"
        />

        <action
                id="Testin.Open"
                class="org.testin.open.OpenAction"
                icon="AllIcons.Actions.MenuOpen"
        />

        <action
                id="Testin.ShowNodeDetails"
                class="org.testin.view.marker.ShowNodeDetailsAction"
                icon="AllIcons.General.IndentDetected"
        />

        <action
                id="Testin.Export"
                class="org.testin.importexport.exports.ExportAction"
                icon="AllIcons.ToolbarDecorator.Export"
        />

        <!-- Declared here rather than in testin-git.xml, so the action exists in
             every IDE and grays itself with the reason when Git is missing -
             the same answer the tree menu gives (#273). -->
        <action
                id="Testin.ViewPendingCommits"
                class="org.testin.git.ViewPendingCommitsAction"
                icon="AllIcons.Actions.Commit"
        />

        <action
                id="Testin.SyncWithRemote"
                class="org.testin.git.SyncActionAction"
                icon="AllIcons.Actions.SyncPanels"
        />

        <action
                id="Testin.Import"
                class="org.testin.importexport.imports.ImportAction"
                icon="AllIcons.ToolbarDecorator.Import"
        />

        <action
                id="Testin.RunTests"
                class="org.testin.runner.RunTestsAction"
                icon="AllIcons.RunConfigurations.TestState.Run"
        />

        <action
                id="Testin.OrderNode"
                class="org.testin.order.OrderNodeAction"
                icon="AllIcons.ObjectBrowser.Sorted"
        />

        <!-- One entry per status the selected node has, generated from its
             enum rather than written out here: a status is a constant on
             ProjectStatus, TestSetStatus or PackageStatus, and a second list of
             them in this file is one that goes stale the day a status is added
             (#175, C9). One group rather than one per kind of node - the node
             says which statuses it has (#110). Compact, so the entries sit
             directly in the Actions menu the way they always have. -->
        <group
                id="Testin.UpdateStatus"
                class="org.testin.explorer.tree.UpdateStatusGroup"
                popup="false"
        />

        <!-- No keyboard-shortcut: DELETE is the tree's key, the card list's and
             the grid's, and one keymap entry would answer for all three. The
             tree binds it on this action instead (#119). -->
        <action
                id="Testin.RemoveNode"
                class="org.testin.remove.RemoveAction"
                icon="AllIcons.Actions.GC"
        />

        <!-- No keyboard-shortcut on either: CTRL+C and CTRL+X are the grid's
             keys for its own cells and the card list's for a test case, and a
             registered shortcut is dispatched before a component's input map -
             so a keymap entry here would answer for all three. The tree binds
             them on these very actions instead (#119). -->
        <action
                id="Testin.CopyNode"
                class="org.testin.clipboard.CopyNodeAction"
                icon="AllIcons.Actions.Copy"
        />

        <action
                id="Testin.CutNode"
                class="org.testin.clipboard.CutNodeAction"
                icon="AllIcons.Actions.MenuCut"
        />

        <action
                id="Testin.PasteNode"
                class="org.testin.clipboard.PasteNodeAction"
                icon="AllIcons.Actions.MenuPaste"
        />

        <action
                id="Testin.Rename"
                class="org.testin.rename.RenameAction"
                icon="AllIcons.Actions.Edit"
        >
            <keyboard-shortcut keymap="$default" first-keystroke="shift F6"/>
        </action>
    </actions>

    <!-- close listeners -->
    <applicationListeners>

        <listener
                class="org.testin.editor.SaveOnProjectClose"
                topic="com.intellij.openapi.project.ProjectCloseListener"
        />

    </applicationListeners>

</idea-plugin>
