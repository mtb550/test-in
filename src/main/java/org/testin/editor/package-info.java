/**
 * The editor tabs a test case and a test run open in: the grid, the card list,
 * the toolbar, the status bar and the file-editor plumbing underneath them.
 * <p>
 * The surface where a tester <i>changes</i> something, which is what tells it
 * from {@link org.testin.view} - the tool window that shows what a node is - and
 * from {@link org.testin.ui}, which is the look both of them are built out of
 * (#110).
 */
package org.testin.editor;
