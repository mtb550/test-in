TESTIN BACKLOG - ITEMS STILL TO REVIEW
======================================
Everything else from the old backlog is now on GitHub: mtb550/test-in issues #2, #6-#37.
What is left below is what was deliberately skipped, plus items that were never reviewed.
Source of truth is GitHub - move things out of this file as they become issues.
Reviewed 11-08-2026.

abbreviations:
project -> p
projectPanel -> pp
tree -> t
name -> s
codeGenerator -> cg
generatorType -> gt


NEVER REVIEWED - decide on these first
======================================

1. move all fonts to setting.
   (issue #19 only syncs the details panel, #12 only covers dialog fonts.
    a global font setting exists nowhere.)

2. add new filter for test execution status. shown only in run editor
   (show: passed, failed, blocked, untested) and not shown in test editor.
   (issue #10 assumes this filter already exists when it adds Out Of Scope to it.)

3. all ui to be updated to the new ui, frameless and buttonless. remove and add and all ui (important)
   (issue #12 covers DIALOGS only. editors, panels and the tree are not covered.)


SKIPPED - kept for a second look
================================

S2. toolbar layout
    make toolbar search on the right.
    remove toolbar texts(details, filter, refresh) and just put the icons.
    add the buttons: reset to filter button on toolbar editor, default to details button.
    reset filter on top of filter list toolbar.(Reset)
    put filter btn to toolbar.
    note: removing labels makes tooltips mandatory, and they must come from Bundle (7 locales).

M13. editor toolbar must read from map, whatever in the map, show it.
     one map all use it: editor toolbar, edit tc popup, view panel, create tc. start one by one.
     @ToolBar put title in toolbar details options and make checked and enabled.
     implement toolbar button interface and enum that allow to add new button later easily.
     unify the filtered list method that in test editor ui and run editor ui (getFilteredList)
     remove getFilteredList() in IEditorUI.
     put the description in details and apply control to hide or show it.
     how to isolate sort in card so you can show or hide.
     note: largest of the group. rebuildGrid() is also duplicated across both editors.
     note: issue #23 (view panel details) deliberately left show/hide description here.

M14. fix step issues:
     1- remove use CTRL+D
     2- add new array item for all cursors not for the last only
     3- remove use quotation in step value
     4- step class not use the splitter class. it is too long
     note: 4 is a refactor, 1-3 are behaviour - could be two issues.

M15. put and use @NotNull in test case dto, clear all related and update create test case
     to put empty not NULL. (major)
     note: this is the natural first package of issue #37 (code quality pass).

M16. sorting number should be fixed in editor once search and list the results. (major)
     note: decide the rule first - is the number the position in the full set, or in the
     filtered view? everything else follows from that.

M19. build edit menu for test run to edit actual result, statuses, put screenshots.
     update edit menu to show the actual result (with base64 screenshot or textarea exception),
     test case run status, or make it not visible if current editor is the test editor
     to not confuse the tester.
     make shortcut to set actual result, screenshot or exception (evidence), test case run status.
     note: overlaps issue #13 - the always-on-top window needs the same paste-evidence and
     set-status paths. build them once.

M21. test run report enhancements:
     A - add JavaScript to switch between dark and white mode.
     B - add new field: project name, branch name, commit id.
     C - add testin logo at left top.
     D - add charts: passed, failed, blocked, untested.
     E - make test run report uneditable to be secure and confidential.
     note: D must land after #10 or the charts inherit the broken counts.
     note: E cannot be enforced in an HTML file the reader owns - PDF or a checksum is the
     closest real option. decide what is actually needed.

M23. implement broadcasting to update project tree.
     Config.getProject().getMessageBus().syncPublisher(TestCaseEventListener.TEST_CASE_ADDED_TOPIC)
           .onTestCaseAdded(newTc);
     note: this is the delivery half of issue #20 - the VFS listener invalidates, the bus
     tells the UI. strong candidate to do right after #20.

M25. performance pass:
     move all hash maps from: Map<UUID, TestCaseDto> idMap = new HashMap<>();
     to: Maps.newHashMapWithExpectedSize(unsortedList.size())   (key-value)
     or: new ArrayList<>(unsortedList.size())                   (numbers, names)
     also refactor the ArrayList usage in the project.
     optional of nullable not recommended inside loop as it creates temp objects in ram.
     instead of passing attributes between methods, use consumer or callback interface.
     note: measure first. the callback change is a design change, not a performance one -
     probably its own issue.

M26. enable RemoveTestProject in the tree, add new action, active, deactivate.
     note: archived projects are currently filtered out of the selector, which is why they
     seem to vanish. issue #8 changes that model - do this after #8.

M27. apply check box once update test case, with unified methods for sanitizing names for
     packages and classes.
     note: the sanitizing half is clear and worth doing alone. explain what the checkbox is
     for - which field, and what it controls.


REMOVED
=======
- fix pi report issues.                                   (dropped 11-08-2026)
- remove sorting in test run and show the path as
  relationship many to many.                              (dropped 11-08-2026)
