project -> p
projectPanel -> pp
tree -> t
name -> s
codeGenerator -> cg
generatorType -> gt

implement mail SMTP notifications.
implement class debugger
implement method debugger
plan & implement delegation between leaders to leaders or leaders to members.
move all fonts to setting.
separate plugin component for execution test cases. small clean quick. (light mode)
test run execution light mode. small plugin can be dragged separate. [important]
add spell checker to correct test cases titles
add dashboards and result in charts for each test run. right click details the show in view panel.
grid view editor.
add new filter for test execution status. shown only in run editor (show: passed, failed, blocked, untested) and not shown in test editor. I will put it to the backlog.
add sorting attribute to view panel in test case details so can update the sort value.
make test case dto configurable.
زر النسخ كتقرير خطأ (Copy Bug Report), [important]

save selected project and hierarchy with selected focus.
enable RemoveTestProject in the tree, add new action, active, deactivate.
open the bug on our open GitHub once test case failed within execution of test run
I think the method runner can run the class. no need for class runner. to be checked.
make step in the first line. make dot and capitalize first letter automatic > steps
make capitalize and dot in test case card and run card. move format method to Tools.
all ui to be updated to the new ui, frameless and buttonless. remove and add and all ui (important)
fix step issues: 1- remove use CTRL+D, 2- add new array item for all cursors not for the last only, 3-remove use quotation in step value, 4- step class not use the splitter class. it is too long

view panel enhancements:
merge created by at and updated by at.

ui enhance:
2-refactor remove test case class: RemoveTestCaseDialog.java

3-implement broadcasting to update project tree.
Config.getProject().getMessageBus().syncPublisher(TestCaseEventListener.TEST_CASE_ADDED_TOPIC).onTestCaseAdded(newTc);});

test editor cards is stored in cache and not disposed. once you close editor and update one of ots test case manually from JSON then re-open the editor,
you will get the old value, that is means the editor reads from memory. to be fixed.

remove sorting in test run and show the path as relationship many to many

make the sorting and unsorted badge run in separate thread.

let us continue, why do you need t retrieve all list as I use paging ? why don't use the paging value to get the first x list, once go next page, load the next x list.

@ToolBar put title in toolbar details options and make checked and enabled.

@BaseCard, put background color for odd and even in Config and use it in all project to unify the UI.

move all hash maps from: (to bypass re-hashing & oversize reserving)
Map<UUID, TestCaseDto> idMap = new HashMap<>();
to:
Maps.newHashMapWithExpectedSize(unsortedList.size()); (for key-value) --no equivalent exists in jdk17 but exist in jdk21
or new ArrayList<>(unsortedList.size()); (for numbers, names)
so also, you need to refactor the ArrayList in project.

optional of nullable not recommended inside loop as it create temp objects in ram within process.

instead of passing attributes between methods, use consumer or callback interface.

editor toolbar must read from map, whatever in the map, show it. one map all use it, editor toolbar, edit tc popup, view panel, create tc. start oe by one.

add the buttons: reset to filter button on toolbar editor, default to details button.

make toolbar search on the right.
remove toolbar texts(details, filter, refresh) and just put the icons.
reset filter on tob of filter list toolbar.(Reset)

pending today 31-04-2026
put filter btn to tool par
unify the filtered list method that in test editor ui and run editor ui (getFilteredList)
put and use @NotNull in test case dto, clear all related and update create test case to put empty not NULL. (major)
implement toolbar button interface and enum that allow to add new button later easily.
sorting number should be fixed in editor once search and list the results. (major)

add shortcut CTRL+F to make focus on toolbar search filed
add shortcut ESCAPE to remove focus in toolbar search filed

apply searchDebounceTimer in BaseToolBar for more stability:
searchDebounceTimer = new Timer(300, e -> callbacks.onFilterChanged());
searchDebounceTimer.setRepeats(false);
@Override
protected void textChanged(final @NotNull DocumentEvent e) {
searchDebounceTimer.restart();
}

question: IntelliJ Task.Backgroundable vs ApplicationManager.getApplication().executeOnPooledThread

parent if abstract to starts with Abstract, otherwise start with Base.

make the search global from project view.
make search bar ui & logic in separate ui in test editor ui & test run ui. make toolbar better,
can add option to search in all test cases, get the test runs that test case added to.

remove getFilteredList() in IEditorUI.

customize run toolbar to add duration and other items to details. pending

put the description in details and apply control to hide or show it.

TO DO: if statement here to be removed as all will title will show and use preferences that store others shown attributes.
backlog: put title in toolbar details options and make checked and enabled.

build edit menu for test run to edit actual result, statuses, put screenshots. also you need to build create login, need to study business first.

how to isolate sort in card so you can show or hide.

add new object: commit id to RunEditorAttributes, it is required in each test run.
work on test run properties, to show the test run configs like commit id and platform and status and language, add right click context menu and toolbar button
on test properties, add charts for conclusion of run, diagrams and by chart, and put it to the report.
in properties, show startedAt, endedAt, total duration.

how to build light wait dialog for executing test cases that can be always on top, show important info,
separate from IntelliJ so I can minimize IntelliJ, and it will keep on top.
show important info and I can click button to select what I can see,
accept CTRL+V to add base64 screenshots or exceptions,
show pass/fail/blocked in proper and big ui.
render to next test case if set the run status.
may be pop up can help, mostly if you can be always on top.

timer:===============
add to dto: startedAt, EndedAt, total duration, path.
add path to run attributes.
update status bar to show the total duration once start the first test case until the last.
once start, change execute icon to stop icon, use white and black icon.
build the logic if stopped, stop status bar duration, stop current test case duration.
if start run, fill started at.
if finish fill ended at.
if stop fill dto duration for test run and current test case.
implement disposer.

update edit menu to show the actual result (with base64 screenshot or textarea exception), test case run status,
or use current edit menu be make it not visible if current editor is the test editor to not make tester confuse.
make shortcut to set actual result, screenshot or exception (evidence, find another word), test case run status.

fix Mac shortcuts. check create test case and generate code on Mac

CTRL+Z to revert back if change description for singe or bulk test cases

compatability with by charm, use option of plugin that make same release matched with by charm and IntelliJ.
install robot framework and try gutter and org.testin.navigate to code.

test cases repository options: local server and connect via ssh, git source, ftp server, no SQL db,

apply check box once update test case. with unify methods for sanitize names for packages and classes

major change, build fast parallel indexer, to retrieve test cases easily. [important].
in test runs,retrieve test cases faster and easier (first, restrict creating test run until commit the current changes).
retrieve test cases easily and faster, as we read tree once in startup time.
test run items will be @jsonIgnore as the JSON should have the ids only then get the objects from cache.

major change, move to tree async model, to manage update, remove, dispose and rename nodes automatic.

fix pi report issues.

in test run, I want to highlight the running test case, by add a new badge (Active) or change the description color for current running.

if change test run to completed then change pending test cases to untested automatic.

revamp ui -> freelancer.

2- we need to store the test in path in XML file because once update the project every thing missed.

4- editor status bar,
remove border of arrows &
add new arrows (go to first button + go to end button) &
reduce arrow padding &
show boarder when hover &
expand pagination field to see value if value is 3 digits &
make max value.

5- test run report enhancements:
    A - add JavaScript to switch between dark and white mode, if you need to change colors please change it.
    B - add new field: project name, branch name, commit id.
    C - add testin logo at left top.
    D - add charts, passes, faild, blocked, untested.
    E - make test run report uneditable to be secure and confidant.


* need to set team members, leaders.

* fix issue: after change test-in path, should update the tree and project list to reads the new path.

* apply XML or YAML file in automation itself.
* make one project each automation project. then update indexer

apply tree async model.
add separate grid view button. (20% Done)
fix all todo comments.
why archived test projects not shown?
remove call rebuildGrid(). just repaint

usage   java swing api      IntelliJ SDK equivalence    api            notes
-------------------------------------------------------------------------------------
  1     JPanel              JBPanel<?>                  (com.intellij.ui.components)        FormBuilder API boundary only
 91     JComponent          — used as-is                (base class, no JB wrapper)         -
  0     JLabel              JBLabel                     (com.intellij.ui.components)        (116 JBLabel in use)
  7     SwingUtilities      ApplicationManager.getApplication()                             static helpers only
 10     JList               JBList                      (com.intellij.ui.components)        renderer/interface params (147 JBList)
  8     JButton             — used as-is                (no JB wrapper)                     -
  0     JScrollPane         JBScrollPane                (com.intellij.ui.components)        (68 JBScrollPane in use)
  3     JTable              JBTable                     (com.intellij.ui.table)             TableCellEditor/Renderer params (38 JBTable)
  0     JComboBox           ComboBox<T>                 (com.intellij.openapi.ui)           converted
  3     JTextField          JBTextField                 (com.intellij.ui.components)        BrowseFolderActionListener<JTextField> generic (35 JBTextField)
  2     JTree               Tree                        (com.intellij.ui.treeStructure)     renderer interface params
  0     JPopupMenu          JBPopupMenu                 (com.intellij.openapi.ui)           converted
  0     JTabbedPane         JBTabbedPane                (com.intellij.ui.components)        converted
  0     JOptionPane         Messages                    (com.intellij.openapi.ui)           none used
  0     JCheckBox           JBCheckBox                  (com.intellij.ui.components)        converted

d. Make font sync work one be inside details (typo: "make font sync work one be inside details" — likely "make font sync work once inside details" or "when inside details").