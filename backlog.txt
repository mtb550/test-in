add new tool window (To Do or Notes) that stored tester temp content.

implement all notifications.

implement class debugger

implement method debugger

persist session for test case explorer and test plan explorer. once open intellij,
explorer should show last opened hierarchy with focus in module.
plan & implement delegation between leaders to leaders or leaders to members.

fixes:
add message that the loading tree in panel
show buttons in test caseeditor tab like test run (run) and (view code)

create action: RemoveTestProject in testGit setting

zoom in, out by mouse and scroll. to allow user to control the size. in editor only.

project panel tree shows the message (nothing to show) once startup on first run. try to change it
to (loading)

implement this in control panel tree:
undoAction
redoAction


update copy and move , drag drop action to prevent copy same file twice in same folder. in project tree
and prevent move test run to test case tree.

change color to gray once cut. in project panel

add static execute method in CreateTestPackage and other methods that call actions direct

make focus on test case tree once open plugin.

add final @NotNull for all methods in project

create the new ui same creation for delete tree node

project activity status to be removed from folder name and use separate xml that has rules

implement details in marker file like created time date by modified, number of test cases , open bugss

add icons to the test case card in the buttom right corner once one click the test case card:
run test method icone, navigate to code icon, ask ai icon

view method code in details panel.

update create new test case ui

try to read the current root node for controller, no need to seach again in test run.
you need just a move to check box object

build changes page to do the commit and push.

enhance the test case editor panel

enhance the test run editor panel

enhance view panel. easy thing you can do it next step.

make automation referrence automatic & create class and packages in project root automatic

change test case and test run editor size by mouse wheel. or make the font control in testGit setting

cusomize test packages to: module, epic, feature

fix disappear expected, id..etc once drag and drop. and make sure tool bar checks are saved once colse then open again

add badge (un-sorted) if the test case not linked to to the before one

faild create test runs, error in path

move editor ui classes to the util, ui package

Summary of list listeners:
MotionListener: Used for your beautiful hover icons (Run / Navigate). NO NEED
AnAction System: Replaces KeyListener for professional, cross-platform keyboard shortcuts (Delete, Copy, Paste).
MouseAdapter (ClickCount==2): Used for Double-Click to open.
** PopupHandler: Replaces manual right-click math for context menus.
** Dynamic Tooltips: Quick peeks at data without clicking.

change structure of test run to be same test set, folder and inside it will be the jsons

bug, after add new test case, it is not linked to the sorting.

add shortcut, if click CTRL+C on the selected test case on editor, then copy test case title. DONE!!

add popup menu in test case editor, group for copy, copy -> title, expected, steps, priority ...etc

how to add offecial listener for status bar

make mouse released after update paging value the click enter

return the code for bgcolor for navigate, run

in code, build icon to move you to open the test case in view panel

move all fonts to setting.

separate plugin component for execution test cases. small clean quick. (light mode)

put shortcuts for passed, failed and blocked in run editor

add toolbar item, show (passed, failed, blocked)

add dictionary to correct test cases titles

add pre condition test case attribute (or query or test data)

generate reports. (in progress)

drag and drop for test runs, multi

add dasboards and result in charts for each test run. right click details the show in view panel.

auto complete steps. (in progress)

grid view editor.

add new filter for test execution status. shown only in run editor (show: passed, failed, blocked, untested) and not shown in test editor. I will put it to the backlog.

open the bug on our onprem github once test case failed within execution of test run

I think the method runner can run the class. no need for class runner. to be checked.

extend intellij disposer to save tree state and open editors once close to re-open after run intellij again
// use activity intellij class to open the editors once run directly
//public class TestGitStartupActivity implements ProjectActivity {}

add sorting attribute to view panel in test case details so can update the sort value.

make automation referrence auto added once create the test case.

make created by auto added once create the test case.

make modified by auto added once update the test case.

test run execution light mode. small plugin can be dragged separate.

update creating test case ui.

extend space of pagination text field value to show 3 digits clearly.

add a button once hover on label, show copy button and accept CTRL+C

change test case dto id value form string to UUID, big change, to be implemented in last sprint.

generate test run report.

make test case dto configurable.

listener between editors correpted, not show the selected in view panel once change editor.

زر النسخ كتقرير خطأ (Copy Bug Report)

persist save action.

make step in the first line. make dot and capitalaize first letter automatic > steps
make capitalize and dot in test case card and run card. move format method to Tools.

implement progress icon in all 3 components. tree, editor, view

update editor search to search by id (UUID)

all ui to be updated to the new ui, frameless and buttonless. remove and add and all ui (important)

fix step issues: 1- remove use CTRL+D, 2- add new array item for all cursors not for the last only, 3-remove use cotation in step value, 4- step class not use the splitter class. it is too long

view panel enhancments:
merge created by at and updated by at.

try it. intellij hint.
JBPopupFactory.getInstance()
                .createHtmlTextBalloonBuilder(SUCCESS_MESSAGE, MessageType.INFO, null)
                .setFadeoutTime(HINT_FADEOUT_MS)
                .createBalloon()
                .show(RelativePoint.getCenterOf(component), Balloon.Position.above);

ui enhance:
1-
persist edit. (pending). remove save method in section classes,
instead use persistance manages in action consumer.

2-
refactor remove test case class: RemoveTestCaseDialog.java

3-
implement broadcating to update project tree.
Config.getProject().getMessageBus().syncPublisher(TestCaseEventListener.TEST_CASE_ADDED_TOPIC).onTestCaseAdded(newTc);});

test editor cards is stored in cache and not disposed. once you close editor and update one of ots test case manually from json then re open the editor,
you will get the old value, that is means the editor reads from momery. to be fixed.

remove sorting in test run and show the path as relationship many to many

make the sorting and unsorted badge run in separate thread.

add actual result to test run and use it.

let us continue, why do you need t retieve all list as I use paging ? why dont use the paging value to get the first x list, once go next page, load the next x list.

@ToolBar put title in tool bar details options and make checked and enabled.

@BaseCard, put backgroound color for odd and even in Config and use it in all project to unify the UI.

@all classes, how to remove null checks or reduce it ? use empty strings in json files.
another solution, here new code:
        Optional.ofNullable(ViewToolWindowFactory.getViewPanel()).ifPresent(ViewPanel::reset);
old code:
ViewPanel viewer = ViewToolWindowFactory.getViewPanel();
        if (viewer != null) {
            viewer.reset();
        }
also, will reduce @Nullable and put @NotNull

remove most of static in project.

remove metadata and put make its logic inside run editor.

refactor sorting logic, unsorted are the remain in the list, no need for the loop again.

move all hash maps from: (to bypass re-hashing & over size reserving)
Map<UUID, TestCaseDto> idMap = new HashMap<>();
to:
Maps.newHashMapWithExpectedSize(unsortedList.size()); (for key-value) --no equivelant exists in jdk17 but exist in jdk21
or new ArrayList<>(unsortedList.size()); (for numbers, names)
so also, you need to refactor the ArrayList in project.

TestEditorUI (review pending) refactor sorting logic

optional of nullable not recommended inside loop as it create temp objects in ram within process.

istead of passing attributes between methods, use consumer or callback interface.

editor toolbar must read from map, whatever in the map, show it. one map all use it, editor tool bar, edit tc popup, view panel, create tc. start oe by one.

add the buttons: reset to filter button on tool bar editor, default to details button.

make tool bar search on the right.
remove tool bar texts(details, filter, refresh) and just put the icons.
reset filter on tob of filter list toolbar.(Reset)

pending today 31.04.2026
put filter btn to tool par
unify the filteredlist method that in test editor ui and run editor ui (getFilteredList)
put and use @NotNull in test case dto and clear all related and update create test case to put empty not null. (major)
implement tool bar button interface and enum that allow to add new buton later easely.
sorting number should be fixed in editor once search and list the results. (major)

add shortcut CTRL+> and CTRL+< to navigate between test or run editors pages
add shortcut CTRL+F to make focus on tool bar search filed
add shortcut ESCAPE to remove focus in tool bar search filed
disable F2 and edit test case button on context menu if the active editor is run editor.

fix create test run issue. (resolved)

keep create test case popup open if click ouside to prevent miss written data

make the search global from project view.

apply searchDebounceTimer in BaseToolBar for more stabability:
searchDebounceTimer = new Timer(300, e -> callbacks.onFilterChanged());
searchDebounceTimer.setRepeats(false);
@Override
protected void textChanged(final @NotNull DocumentEvent e) {
searchDebounceTimer.restart();
}


question: intellij Task.Backgroundable vs ApplicationManager.getApplication().executeOnPooledThread

change all interfaces to starts with I
parent if abstract to starts with Abstract, otherwise start with Base.

fix change viow panel to show selected test case if change editor.

add test run duration
make tool bar deails checkable one click anywhere without need to exactly click check box.
make tool bar filter auto hide once move mouse outside.
make search bar logic in separate thread in initiate in test editor ui & test run ui.
remove edit test case in test run
remove getFilteredList() in IEditorUI.
customize run tool bar to add duration and other items to details. pending
selcet on test case after create it.
put the description in details and apply controll to hide or show it.

move updateUi from test card and run card to its rederer.
/// TODO: if statement here to be removed as all will title will shown and use preferrences that store others shown attributes.
/// backlog: put title in tool bar details options and make checked and enabled.
add test case path to run card.
add new object: commit id to RunEditorAttributes, it is required in each test run.
in tool bar, add filtering by empty group or not setted group.
work on test run properties, to show the test run configs like commit id and platform and status and language, add right click context menu and tool bar button