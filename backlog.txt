add new tool window (To Do or Notes) that stored tester temp content.

implement all notifications.

implement class debugger

implement method debugger

persist session for test case explorer and test plan explorer. once open IntelliJ,
explorer should show last opened hierarchy with focus in module.
plan & implement delegation between leaders to leaders or leaders to members.

fixes:
add message that the loading tree in panel
show buttons in test case editor tab like test run (run) and (view code)

create action: RemoveTestProject in testin setting

zoom in, out by mouse and scroll. to allow user to control the size. in editor only.

project panel tree shows the message (nothing to show) once startup on first run. try to change it
to (loading)

implement this in control panel tree:
undoAction
redoAction


update copy and move , drag drop action to prevent copy same file twice in same folder. in project tree
and prevent move test run to test case tree.

change color to gray once cut. in project panel

add static execute method in CreateTestPackage and other methods that call actions direct

make focus on test case tree once open plugin.

add final @NotNull for all methods in project

create the new ui same creation for delete tree node

project activity status to be removed from folder name and use separate XML that has rules

implement details in marker file like created time date by modified, number of test cases , open bugs

add icons to the test case card in the bottom right corner once one click the test case card:
run test method icon, navigate to code icon, ask AI icon

view method code in details panel.

update create new test case ui

try to read the current root node for controller, no need to search again in test run.
you need just a move to check box object

build changes page to do the commit and push.

enhance the test case editor panel

enhance the test run editor panel

enhance view panel. easy thing you can do it next step.

make automation reference automatic & create class and packages in project root automatic

change test case and test run editor size by mouse wheel. or make the font control in testin setting

customize test packages to: module, epic, feature

fix disappear expected, id.etc. once drag and drop. and make sure toolbar checks are saved once close then open again

add badge (un-sorted) if the test case not linked to the before one

failed create test runs, error in path

move editor ui classes to the util, ui package

Summary of list listeners:
MotionListener: Used for your beautiful hover icons (Run / Navigate). NO NEED
AnAction System: Replaces KeyListener for professional, cross-platform keyboard shortcuts (Delete, Copy, Paste).
MouseAdapter (ClickCount==2): Used for Double-Click to open.
** PopupHandler: Replaces manual right-click math for context menus.
** Dynamic Tooltips: Quick peeks at data without clicking.

change structure of test run to be same test set, folder and inside it will be the JSONs

bug, after add new test case, it is not linked to the sorting.

add shortcut, if click CTRL+C on the selected test case on editor, then copy test case title. DONE!!

add popup menu in test case editor, group for copy, copy -> title, expected, steps, priority ...etc

how to add official listener for status bar

make mouse released after update paging value the click enter

return the code for bg color for navigate, run

in code, build icon to move you to open the test case in view panel

move all fonts to setting.

separate plugin component for execution test cases. small clean quick. (light mode)

put shortcuts for passed, failed and blocked in run editor

add toolbar item, show (passed, failed, blocked)

add dictionary to correct test cases titles

add pre-condition test case attribute (or query or test data)

generate reports. (in progress)

drag and drop for test runs, multi

add dashboards and result in charts for each test run. right click details the show in view panel.

auto complete steps. (in progress)

grid view editor.

add new filter for test execution status. shown only in run editor (show: passed, failed, blocked, untested) and not shown in test editor. I will put it to the backlog.

open the bug on our open GitHub once test case failed within execution of test run

I think the method runner can run the class. no need for class runner. to be checked.

extend IntelliJ disposer to save tree state and open editors once close to re-open after run IntelliJ again
// use activity IntelliJ class to open the editors once run directly
//public class testinStartupActivity implements ProjectActivity {}

add sorting attribute to view panel in test case details so can update the sort value.

make automation reference auto added once create the test case.

make created by auto added once create the test case.

make modified by auto added once update the test case.

test run execution light mode. small plugin can be dragged separate.

update creating test case ui.

extend space of pagination text field value to show 3 digits clearly.

add a button once hover on label, show copy button and accept CTRL+C

change test case dto id value form string to UUID, big change, to be implemented in last sprint.

generate test run report.

make test case dto configurable.

listener between editors corrected, not show the selected in view panel once change editor.

زر النسخ كتقرير خطأ (Copy Bug Report)

persist save action.

make step in the first line. make dot and capitalize first letter automatic > steps
make capitalize and dot in test case card and run card. move format method to Tools.

implement progress icon in all 3 components. tree, editor, view

all ui to be updated to the new ui, frameless and buttonless. remove and add and all ui (important)

fix step issues: 1- remove use CTRL+D, 2- add new array item for all cursors not for the last only, 3-remove use quotation in step value, 4- step class not use the splitter class. it is too long

view panel enhancements:
merge created by at and updated by at.

try it. IntelliJ hint.
JBPopupFactory.getInstance()
                .createHtmlTextBalloonBuilder(SUCCESS_MESSAGE, MessageType.INFO, null)
                .setFadeoutTime(HINT_FADEOUT_MS)
                .createBalloon()
                .show(RelativePoint.getCenterOf(component), Balloon.Position.above);

ui enhance:
1-
persist edit. (pending). remove save method in section classes,
instead use persistence manages in action consumer.

2-
refactor remove test case class: RemoveTestCaseDialog.java

3-
implement broadcasting to update project tree.
Config.getProject().getMessageBus().syncPublisher(TestCaseEventListener.TEST_CASE_ADDED_TOPIC).onTestCaseAdded(newTc);});

test editor cards is stored in cache and not disposed. once you close editor and update one of ots test case manually from JSON then re-open the editor,
you will get the old value, that is means the editor reads from memory. to be fixed.

remove sorting in test run and show the path as relationship many to many

make the sorting and unsorted badge run in separate thread.

add actual result to test run and use it.

let us continue, why do you need t retrieve all list as I use paging ? why don't use the paging value to get the first x list, once go next page, load the next x list.

@ToolBar put title in toolbar details options and make checked and enabled.

@BaseCard, put background color for odd and even in Config and use it in all project to unify the UI.

@all classes, how to remove null checks or reduce it ? use empty strings in JSON files.
another solution, here new code:
        Optional.ofNullable(ViewToolWindowFactory.getViewPanel()).ifPresent(ViewPanel::reset);
old code:
ViewPanel viewer = ViewToolWindowFactory.getViewPanel();
        if (viewer != null) {
            viewer.reset();
        }
also, will reduce @Nullable and put @NotNull

remove most static in project.

remove metadata and put make its logic inside run editor.

refactor sorting logic, unsorted are the remain in the list, no need for the loop again.

move all hash maps from: (to bypass re-hashing & oversize reserving)
Map<UUID, TestCaseDto> idMap = new HashMap<>();
to:
Maps.newHashMapWithExpectedSize(unsortedList.size()); (for key-value) --no equivalent exists in jdk17 but exist in jdk21
or new ArrayList<>(unsortedList.size()); (for numbers, names)
so also, you need to refactor the ArrayList in project.

TestEditorUI (review pending) refactor sorting logic

optional of nullable not recommended inside loop as it create temp objects in ram within process.

instead of passing attributes between methods, use consumer or callback interface.

editor toolbar must read from map, whatever in the map, show it. one map all use it, editor toolbar, edit tc popup, view panel, create tc. start oe by one.

add the buttons: reset to filter button on toolbar editor, default to details button.

make toolbar search on the right.
remove toolbar texts(details, filter, refresh) and just put the icons.
reset filter on tob of filter list toolbar.(Reset)

pending today 31.04.2026
put filter btn to tool par
unify the filtered list method that in test editor ui and run editor ui (getFilteredList)
put and use @NotNull in test case dto and clear all related and update create test case to put empty not null. (major)
implement toolbar button interface and enum that allow to add new button later easily.
sorting number should be fixed in editor once search and list the results. (major)

add shortcut CTRL+F to make focus on toolbar search filed
add shortcut ESCAPE to remove focus in toolbar search filed

apply searchDebounceTimer in BaseToolBar for more stability:
searchDebounceTimer = new Timer(300, e -> callbacks.onFilterChanged());
searchDebounceTimer.setRepeats(false);
@Override
protected void textChanged(final @NotNull DocumentEvent e) {
searchDebounceTimer.restart();
}


question: IntelliJ Task.Backgroundable vs ApplicationManager.getApplication().executeOnPooledThread

parent if abstract to starts with Abstract, otherwise start with Base.

make the search global from project view.
make search bar ui & logic in separate ui in test editor ui & test run ui. make toolbar better,
can add option to search in all test cases, get the test runs that test case added to.

remove getFilteredList() in IEditorUI.

customize run toolbar to add duration and other items to details. pending

put the description in details and apply control to hide or show it.

/// TODO: if statement here to be removed as all will title will show and use preferences that store others shown attributes.
/// backlog: put title in toolbar details options and make checked and enabled.

add test case path to run card.

open test run directly after save.

build commit

build edit menu for test run to edit actual result, statuses, put screenshots. also you need to build create login, need to study business first.

how to isolate sort in card so you can show or hide.

persist save test run inputs.

disable start execution if test run status is completed.

add new object: commit id to RunEditorAttributes, it is required in each test run.
work on test run properties, to show the test run configs like commit id and platform and status and language, add right click context menu and toolbar button
on test properties, add charts for conclusion of run, diagrams and by chart, and put it to the report.
in properties, show startedAt, endedAt, total duration.

how to build light wait dialog for executing test cases that can be always on top, show important info,
separate from IntelliJ so I can minimize IntelliJ, and it will keep on top.
show important info and I can click button to select what I can see,
accept CTRL+V to add base64 screenshots or exceptions,
show pass/fail/blocked in proper and big ui.
render to next test case if set the run status.
may be pop up can help, mostly if you can be always on top.

------------------------------------
timer:===============
add to dto: startedAt, EndedAt, total duration, path.
add path to run attributes.
update status bar to show the total duration once start the first test case until the last.
once start, change execute icon to stop icon, use white and black icon.
build the logic if stopped, stop status bar duration, stop current test case duration.
if start run, fill started at.
if finish fill ended at.
if stop fill dto duration for test run and current test case.
implement disposer.

update edit menu to show the actual result (with base64 screenshot or textarea exception), test case run status,
or use current edit menu be make it not visible if current editor is the test editor to not make tester confuse.
make shortcut to set actual result, screenshot or exception (evidence, find another word), test case run status.

how to make timer is accurate
--------------

fix mac shortcuts.

--------------
new magicked features, copy or cut test cases from specific editor and paste it in another editor.
--------------

add new item in create test case ui: query or pre-condition.

compatability with by charm, use option of plugin that make same release matched with by charm and IntelliJ.
install robot framework and try gutter and navigate to code.

amazing feature: select multi test set then open together it in the editor.
drag and drop for project panel to editor panel

remain enhancements for create new node ui.
add check box (create folder) in create new node
add status bar to create new node shows: enter:save scape:cancel
regex, no dot or special chars